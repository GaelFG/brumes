package fr.gembasher.brumes

import fr.gembasher.brumes.server.Zone 

/**
* A connected player character
*/
class Character(val id :Int, var name :String, val position :Position, var velocity :Double ) extends Entity( ) {
	
}